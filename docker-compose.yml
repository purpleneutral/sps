services:
  scanner:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - scanner-data:/data
    environment:
      - DATABASE_URL=sqlite:///data/scanner.db
      - RUST_LOG=info
      - SPS_API_KEY=${SPS_API_KEY:-}
      - SPS_CORS_ORIGINS=${SPS_CORS_ORIGINS:-https://seglamater.app,https://seglamater.com}
    restart: unless-stopped

volumes:
  scanner-data:
